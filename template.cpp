#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef double fd;
int minn=1e9;
#define f(j,a,n) for(int j=a;j<n;j++)
#define fast 	ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
int main(){
    fast;
    int t;
    cin>>t;
    while(t--)
    {
        int n ;
        cin>>n;
        vector<int>res(n);

    }
}